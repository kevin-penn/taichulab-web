import Link from "next/link";

const Home = () => {
  return (
    <div className="min-h-screen min-w-screen" style={{
      backgroundImage: `url('/imgs/bg-first.png')`,
      backgroundSize: "cover",
      backgroundPosition: "center",
      backgroundRepeat: "no-repeat",
    }}>
     
    </div>
  );
};

export default Home;
